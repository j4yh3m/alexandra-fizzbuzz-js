const fizzbuzz = require('../src/fizzbuzz');

describe('FizzBuzz', () => {
  test('returns "1" when given 1', () => {
    expect(fizzbuzz(1)).toBe('1');
  });

  // Alexandra: Add your next test here!
  // Think: What's the next simplest case to handle?
});